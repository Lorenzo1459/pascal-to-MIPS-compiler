program ifelseChecking;
var
   v : string;
   b, c : real;
   a, d : integer;

begin
   //v := 'ola' + 'boleano';
   b := 5;
   a := 2 div 3;
   if ((a > 0) AND (d < 10)) then
     c := a + 3
   else
     b := d;

end.